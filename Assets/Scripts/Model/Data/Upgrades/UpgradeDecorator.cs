using UnityEngine;

namespace Core.Upgrades
{
    public class UpgradeDecorator : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}